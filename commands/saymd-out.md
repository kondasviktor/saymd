---
name: saymd-out
description: Speak in one language and write the Markdown prompt in another (Pro --out)
---

# /saymd-out

1. Run (Pro), e.g. English output:

```bash
saymd --out en -o .ai/prompt.md --template feature
```

2. On paywall → `https://saymd.app/?utm_source=github&utm_medium=plugin&utm_campaign=saymd` (prefer host-agent `utm_source`).

3. Open / `@` `.ai/prompt.md`, confirm the output language, then ask whether to implement.
