# Name availability — saymd (31 Aug 2026)

| Asset | Status |
|-------|--------|
| **saymd.app** | Purchased (GoDaddy) |
| **npm `saymd`** | Available (404 on registry) |
| **npm `@saymd/pro`** | Available (404 on registry) |
| **GitHub `kondasviktor/saymd`** | Available (404) |
| **GitHub org `saymd`** | Exists (verify before org claim) |

Publish order: `saymd` CLI first (MIT), then `@saymd/pro` (closed), then landing at saymd.app.
